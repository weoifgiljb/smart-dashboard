---
description: 前端 TS/Vue：靠推断、禁止 any、闭集用枚举、模板不用 v-for。新写或改 Vue/TS 时必须遵守。
globs: frontend/**/*.{vue,ts,tsx}
alwaysApply: false
---

# 前端类型与模板

## 类型

- **能推断就别标**：`const n = 1`，不要 `const n: number = 1`。只在函数入参、公有返回、收窄失败时写类型。
- **禁止 `any` / `as any`**。未知数据用 `unknown` 再收窄；对象用接口或泛型；索引用 `Record<Enum | string, T>`。
- **闭集用 `enum`**（主题、热力档、任务状态、番茄类型），不要散落 `'dark' | 'light'` 字符串。`switch` 必须穷尽，`default` 用 `never`。

```ts
enum ThemeName {
  Light = 'light',
  Dark = 'dark',
}

function applyTheme(theme: ThemeName) {
  document.documentElement.dataset.theme = theme
}
```

## 模板禁止 `v-for`

- 不要写 `v-for`。固定少量节点在模板里写死。
- 动态列表：在 `<script>` 里 `.map` + `h()`，或拆成 **setup 返回 render 的具名子组件**，模板只挂组件。
- 禁止 `v-for` + `v-if` 写在同一元素上。

### 组件 VNode 禁止插值（会炸页面）

**现象**：日记页控制台 `TypeError: Converting circular structure to JSON`，列表整页空白、只剩 loading，过去日记也看不见。

**原因**：模板 `{{ tagNodes }}` 或 `<component :is="h(ElTimelineItem)">` 会走 `JSON.stringify`。`h(ElTag)` / `h(ElTimelineItem)` 这类组件 VNode 有 `vnode ↔ component` 循环引用，stringify 直接抛错，后续渲染中断。

**解决**：拆成 setup 返回 render 的真实子组件（`DiaryTags.vue`、`DiaryTimeline.vue`），模板只写 `<DiaryTags :tags="tags" />`，不要把组件 VNode 插进 mustache。

```ts
// ❌ 模板 {{ tagNodes }}、:is="h(ElTimeline, ...)"
const tagNodes = tags.map((tag) => h(ElTag, { key: tag }, () => tag))

// ✅ 具名子组件：setup 里 return () => h(...)，模板 <DiaryTags :tags="tags" />
```

原生 `h('span')` 仍可插值；**Element Plus / 自定义组件 VNode 一律走 render 子组件**，不要插进 mustache。
